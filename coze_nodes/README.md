# Coze 节点库索引

本目录用于保存用户提供的 Coze（扣子）节点素材，并整理成后续测试、说明和复用已创建工作流的参考索引。

用户也可以把这些节点内容直接复制粘贴到 ChatGPT 网页端，让 ChatGPT 根据节点内容辅助操作、分析、测试或整理“生成生图 Prompt”的工作流。

## 来源

- 来源文件：`raw/7645250322341134390-coze-nodes-clipboard.txt`
- 来源类型：Coze 工作流画布剪贴板数据
- workflow_id：`7645250322341134390`
- host：`www.coze.cn`
- 记录日期：2026-05-30

## 重要说明

- 原始内容是 Coze 画布剪贴板数据，不是严格 JSON；里面存在 Coze 前端内部简写字段。
- 原始内容里 `edges` 为空，所以它更像“节点素材集合”，不是已经连好线的完整业务流程。
- 后续测试或说明工作流时，应根据用户需求，从 `PATH_INDEX.md` 选择合适路径，再按 `NODE_INDEX.md` 补节点入参、出参和异常处理。
- 不要把本目录里的示例字段当成已经实测的 Coze API Output Schema；真实接口输出仍需在 `WORKFLOWS.md` 中按实测结果登记。

## 文件说明

| 文件 | 作用 |
|---|---|
| `NODE_INDEX.md` | 节点能力索引，记录每个节点大概做什么、关键入参、关键出参和注意事项。 |
| `PATH_INDEX.md` | 可复用工作流路径索引，用于把节点快速组合成文生图、图生图、抠图、画质提升等流程。 |
| `raw/7645250322341134390-coze-nodes-clipboard.txt` | 用户提供的原始 Coze 节点剪贴板内容，保留证据和细节。 |

## 后续使用方式

1. 用户提出测试或整理目标，例如“测试这个生成生图 Prompt 的工作流”。
2. 先查 `WORKFLOWS.md`，确认是否已经有真实 `workflow_id`、调用接口和实测记录。
3. 如需理解节点能力，再查 `PATH_INDEX.md` 和 `NODE_INDEX.md`。
4. 如果用户把节点复制到 ChatGPT 网页端操作，应把节点内容整理成清晰的输入、处理、输出和测试步骤。
5. 形成新的调用结果或实测结论后，登记到项目根目录的 `WORKFLOWS.md`。
